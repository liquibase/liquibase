package liquibase;

/**
 * Test suite for all Snowflake extension tests.
 * Note: JUnit 5 Suite dependencies not available, run tests individually:
 * 
 * mvn test -Dtest="*CreateSchema*"
 * mvn test -Dtest="*DropSchema*" 
 * mvn test -Dtest="ServiceRegistrationTest"
 */
public class SnowflakeExtensionTestSuite {
    // Placeholder - run tests individually until JUnit 5 Suite is available
}