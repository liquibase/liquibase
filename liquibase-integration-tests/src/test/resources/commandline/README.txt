This folder exists to easily test the built liquibase jar.  

Simply copy the liquibase.jar and liquibase.bat scripts to this directory and run them.

There are pre-existing liquibase.properties file for different databases.

For example:

liquibase --defaultsFile=liquibase.h2.properties update